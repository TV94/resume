
 {{#with navigation}}
        <div class="card mb-3">
            <div class="col-12">
                <div class="nav">
        {{#each links}}
                <div class="nav-item">
                    <a class="nav-link active" aria-current="page" href={{href}}>{{text}}</a>
                </div>
        {{/each}}
                    <a class="nav-link disabled">Disabled</a>
                </div>
            </div>
        </div>
    {{/with}}
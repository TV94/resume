
<hr/>
        <div class="row">
            <div class="nav nav-flex-column">
    {{#each footer}}
            <ul>
                <div class="nav-item text-sm">
                    {{#each this}}
                        {{#if @last}}   
                    <a class="nav-link disabled" aria-current="page" href={{link}}>{{text}}</a>
                    
                        {{else}}
                    <a class="nav-link" aria-current="page" href={{link}}>{{text}}</a>
                        {{/if}}
                    {{/each}}
                </div>
            </ul>
    {{/each}}
    </div>
    </div>